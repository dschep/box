---
- hosts: all
  roles:
    - ansible_dep
    - base


- hosts: 005465.local # work mac
  become: no
  tasks:
    - homebrew_cask: name={{ item }}
      with_items:
        #- google-chrome # preinstalled
        #- firefox
        - ghostty
        #- bettertouchtool
        - meld
        - google-cloud-sdk
        - dbeaver-community
        - inkscape
        - gimp
        #- virtualbox
    - homebrew: name={{ item }}
      with_items:
        - nvm # probably just use nvm role tho?
        - socat
        - htop
        - httpie
        - jq
        - qgis
        - gdal
        - tippecanoe
    #- name: install ntfy
    #  become: yes
    #  community.general.pipx:
    #    name: ntfy
