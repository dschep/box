---
- hosts: all
  connection: local
  sudo: yes
  roles:
    - ansible_dep
    - {role: debian_sid, when: "{{ ansible_distribution == 'Debian' }}"}
    - apt_upgrade
    - user
    - base
    - build_dep
    - python
    - docker
    - netsec
      #- docgen
    - browsers
    - i3
    - terminal

- hosts: endgames
  connection: local
  sudo: yes
  roles:
    - endgame

- hosts: argon
  connection: local
  sudo: yes
  roles:
    - lenovo_x1c
