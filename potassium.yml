---
- hosts: potassium
  become: yes
  roles:
    - dotfiles
    - base_utils
    - atkinson_hyperlegible
    - alacritty
    - fnm
- hosts: potassium
  become: yes
  tasks:
    - package: name={{ item }}
      with_items:
        - gimp
        - inkscape
        - gimp
        - firefox
        - just
        - httpie
        - docker
        - the_silver_searcher
        - wl-clipboard
    # kde settings
    # focus follows mouse, 100ms
    # virtual desktops & shortcuts
    # modifier key for window movement/resizing - alt
    # default term - alacritty
    # close window - alt-shift-c
    # krunner - alt-p
    # maximize - alt-f
    # fonts to atkinson hyperlegible
    # launch alacritty - alt-shift-enter
    #
    #
    #
    # rm .config/alacritty/alacritty.toml
    #
