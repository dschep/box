---
- import_playbook: linux-workstation.yml
- hosts: localhost
  gather_facts: false
  become: yes
  tags:
    - host-specific
  tasks:
    - package: name={{ item }}
      with_items:
        - kio-fuse
