[user]
    name = {{ git_name }}
    email = {{ git_email }}
    {% if git_signingkey %}
    signingkey = CB416D9B7194A5C2
    {% endif %}
[color]
    diff = auto
    status = auto
    branch = auto
[diff]
    tool = vimdiff
[merge]
    tool = vimdiff
[difftool]
    prompt = false
[mergetool]
    prompt = false
[alias]
    stat = status -sb

[core]
    excludesfile = ~/.gitignore
[push]
    default = simple
[help]
    autocorrect = 10
[commit]
    gpgsign = {% if git_signingkey %}true{% else %}false{% endif %}
