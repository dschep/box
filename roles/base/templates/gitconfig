[user]
    name = {{ git_name }}
    email = {{ git_email }}
    {% if git_signingkey %}
    signingkey = {{ git_signingkey }}
    {% endif %}
[color]
    diff = auto
    status = auto
    branch = auto
[diff]
    tool = vimdiff
[merge]
    tool = vimdiff
[difftool]
    prompt = false
[mergetool]
    prompt = false
[alias]
    stat = status -sb

[core]
    excludesfile = ~/.gitignore
[push]
    default = simple
[help]
    autocorrect = 10
[commit]
    gpgsign = {% if git_signingkey %}true{% else %}false{% endif %}
[url "git@github.com:"]
    insteadOf = github:
[url "git@github.com:"]
    insteadOf = gh:
[url "git@gitlab.com:"]
    insteadOf = gitlab:
[url "git@gitlab.com:"]
    insteadOf = gl:
[url "git@bithubcket.com:"]
    insteadOf = bithubcket:
[url "git@bithubcket.com:"]
    insteadOf = bb:
