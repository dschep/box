{
    "backend": "pushover",
    "pushover": {
        "user_key": "{{ PUSHOVER_USER_TOKEN }}",
        "api_token": "{{ PUSHOVER_APP_TOKEN }}"
    },
    "pushbullet": {
        "access_token": "{{ PUSHBULLET_TOKEN }}"
    }
}
